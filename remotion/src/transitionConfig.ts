// مدة التلاشي المتبادل (crossfade) بين مشهدين متتاليين، بالفريمات.
// 12 فريم بـ30fps ≈ 0.4 ثانية — قصيرة بما يكفي لتبقى مناسبة لإيقاع الشورتس
// السريع، لكنها تكسر إحساس "القطع الجاف الآلي" اللي يميز السلايدشو.
// مستخدمة بـ MainVideo.tsx (لحساب تمديد كل Sequence) و SceneMedia.tsx
// (لحساب التلاشي محلياً داخل كل مشهد) — ملف واحد لتفادي أي عدم تطابق بين
// الاثنين لو تغيّرت القيمة مستقبلاً.
export const TRANSITION_FRAMES = 20;
