You are doing Global Topic Understanding for a short-form video, BEFORE
any individual scene is analyzed. Every scene's media search will
inherit what you produce here, so be specific and decisive.

Video topic: {topic}
Full narration: "{narration}"

Return ONLY JSON, no markdown fences, in exactly this shape:
{{
  "scientific_domain": "<the single field/discipline this video lives in, e.g. 'Oceanography', 'Astrophysics', 'Neuroscience'>",
  "visual_theme": "<short theme label, e.g. 'Deep Sea', 'Outer Space', 'Human Brain'>",
  "visual_style": "<e.g. 'Scientific Simulation', 'Real World Nature Footage', 'Studio/Talking Head'>",
  "preferred_environment": "<the one setting most scenes should visually live in>",
  "color_palette": "<e.g. 'Dark blues and blacks', 'Warm oranges', 'Clinical white/blue'>",
  "lighting_style": "<e.g. 'Dark', 'Bright natural', 'Studio lighting'>",
  "forbidden_domains": ["<3-6 entirely different subject domains that must NOT appear unless the script explicitly moves there, e.g. 'Politics', 'Office', 'Podcast', 'City Traffic'>"],
  "forbidden_objects": ["<4-8 concrete objects/settings wrong for this whole video, e.g. 'Microphone', 'Meeting Room', 'Beach', 'Coral Reef'>"]
}}

Rules:
- This is about the ENTIRE video, not one sentence — stay general enough to apply to every scene, unless the narration itself clearly moves between domains (e.g. an ocean-to-space comparison), in which case still name the ONE primary domain here; per-scene exceptions are handled separately.
- Keep every list item short (1-3 words).
- Return JSON only, no explanation.
